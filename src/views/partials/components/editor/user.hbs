<article>
  <img src="{{data.user.profile_image.medium}}" alt="Photo of {{data.user.name}}">
  <div>
    <h3>{{data.user.name}}</h3>
    <h4>{{data.user.total_photos}} Photos</h4>
  </div>
</article>

<article id="buttonWrapper" >
  <div id="buttons">
    <button id="downBtn">Download</button>
    <button id="saveBtn">Save</button>
  </div>
</article>

